/**
 * * 모든 page 에 만들어도 상관없음
 *
 *  react 의 <Suspence /> 와 똑같은 역할
 *
 * * client component, server component 모두 사용 가능
 *
 * @returns
 *
 */
export default function Loading() {
  return (
    <div>
      <h4>로딩중!!!!!!</h4>
    </div>
  );
}
